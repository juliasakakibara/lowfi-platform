{
  "name": "stick-man-birthday-game",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "prebuild": "node scripts/generate-sounds.js && node scripts/copy-pixel-platformer.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "kaboom": "^3000.1.17"
  },
  "devDependencies": {
    "pngjs": "^7.0.0",
    "vite": "^6.3.5"
  }
}
